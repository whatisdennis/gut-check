// External wiring. Left blank for the MVP; fill these in to go live.
// - BOOKING_URL: Dennis's Calendly link for the paid teardown session.
// - FORM_ENDPOINT: a single form endpoint (e.g. Formspree) for email capture.
// Both degrade gracefully when empty: the booking CTA shows but does nothing
// destructive, and email capture confirms locally without posting anywhere.

export const BOOKING_URL = "https://cal.com/dennisdelgado/product-strategy-call";
export const FORM_ENDPOINT = "";
